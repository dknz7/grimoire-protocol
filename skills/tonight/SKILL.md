---
name: tonight
description: "Alias for /today (evening mode). The unified check-in skill handles all time-of-day modes. This exists for backward compatibility with !goodnight and !tonight triggers."
---

# /tonight — Redirects to /today (Evening Mode)

This skill has been absorbed into the unified `/today` skill. When triggered, invoke `/today` — it will automatically detect the evening time window and use evening mode.

**Do not run separate logic here.** Just invoke the `today` skill.
